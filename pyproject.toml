[project]
name = "nodus"
version = "0.3.0"
description = "NODUS — Node Orchestration DSL for Unified Systems"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
nodus = "runtime.cli.nodus:main"

[dependency-groups]
dev = [
    "pyright>=1.1.408",
    "pytest>=9.0.2",
    "ruff>=0.15.6",
]
