§wf:ticket_triage v1.0
;; Reference workflow for customer support tickets.

§runtime: {
  core:    .nodus/core/schema.nodus
  mode:    production
}

@ON: new_ticket
@steps:
1. ANALYZE($in.text) ~intent → $intent
2. ROUTE($intent)
